// uriel - Verificar si un arreglo contiene un número específico

// 1. Declaramos el arreglo de números
const numeros = [5, 10, 20, 50, 75, 100, 150];

// 2. Creamos una función que verifica si un número está en el arreglo
function contieneNumero(arreglo, numero) {
    return arreglo.includes(numero);
}

// 3. Verificamos si el arreglo contiene el número 100
if (contieneNumero(numeros, 100)) {
    console.log("El arreglo contiene el número 100 ");
} else {
    console.log("El arreglo NO contiene el número 100 ");
}

// ejercicio resuelto por IRVING URIEL



// calificado irving uriel!!

/*
    instrucciones:
    1. si ya actualizaste tu main, significa que estas biendo estas instrucciones
    2. ahora actualiza tu main2
    3. sube los cambios a tu rama
    4. listo, debes enviarme 3 fotos a mi whasapp personal: 
        a) evidencia del main sobre lo que te puse en la calificación (osea esto 👈)
        b) evidencia del main2 (el otro archivo que actualizaste)
        c) evidencia de tu repositorio donde igualmente se vea lo que te califiqué
*/