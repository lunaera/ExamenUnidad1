// Arreglo de edades
const edades = [15, 22, 30];

// Filtrar solo los mayores de edad (>=18)
const mayoresDeEdad = edades.filter(edad => edad >= 18);

console.log("Mayores de edad:", mayoresDeEdad);
